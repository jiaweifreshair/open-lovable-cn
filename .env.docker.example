# ============================================================================
# Open Lovable - Docker 环境变量配置
# ============================================================================
# ⚠️  安全提醒：
# - 这是生产环境配置模板
# - 请替换所有 'your_xxx' 占位符为真实值
# - 不要提交包含真实密钥的文件到 Git
# - 使用 Docker secrets 或云服务的密钥管理服务
# ============================================================================

# =================================================================================
# FIRECRAWL (三层降级策略 - Layer 1)
# =================================================================================
# Firecrawl 云端爬虫服务 API Key
# 获取方式：https://firecrawl.dev
# 🚫 未配置时：自动降级到 Layer 2 (Crawlee)
FIRECRAWL_API_KEY=your_firecrawl_api_key

# =================================================================================
# AI PROVIDER
# =================================================================================
# OpenAI 兼容接口配置（支持多种 AI 提供商）
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：其他 AI 提供商
# ANTHROPIC_API_KEY=your_anthropic_key
# GROQ_API_KEY=your_groq_key
# GOOGLE_AI_API_KEY=your_google_key

# =================================================================================
# SANDBOX PROVIDER
# =================================================================================
# 代码沙箱提供商（用于代码执行）
# 选项: vercel | e2b
SANDBOX_PROVIDER=e2b

# Vercel Sandbox (可选)
# 需要真实的 OIDC token，通过 `vercel env pull` 获取
# VERCEL_OIDC_TOKEN=your_real_vercel_oidc_token

# E2B Sandbox（推荐使用）
# 获取方式：https://e2b.dev
E2B_API_KEY=your_e2b_api_key

# =================================================================================
# OPTIONAL SERVICES
# =================================================================================
# Morph API（可选功能）
# MORPH_API_KEY=your_morph_key

# =================================================================================
# APPLICATION SETTINGS
# =================================================================================
# Next.js 应用设置
NODE_ENV=production
PORT=3000
NEXT_TELEMETRY_DISABLED=1

# =================================================================================
# PLAYWRIGHT & CRAWLEE (三层降级策略 - Layer 2 & 3)
# =================================================================================
# Playwright 和 Crawlee 通过 Docker 镜像提供，无需额外配置
# Layer 2: Crawlee（自动选择 Cheerio 或 Playwright）
# Layer 3: Playwright（最终兜底）

# Playwright 浏览器缓存路径（Docker 卷管理）
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright

# Crawlee 存储路径（Docker 卷管理）
CRAWLEE_STORAGE_DIR=/app/storage
